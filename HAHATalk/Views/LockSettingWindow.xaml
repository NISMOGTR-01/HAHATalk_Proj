<Window x:Class="HAHATalk.Views.LockSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HAHATalk.Views"
        mc:Ignorable="d"
        Title="잠금모드 암호 설정" 
        Height="450" Width="380"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#111111"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0,8,40,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="BaseBorder"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock x:Name="PlaceholderText" 
                                       Text="{TemplateBinding Tag}"
                                       Foreground="#999999"
                                       FontSize="{TemplateBinding FontSize}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,0"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FEE500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ConfirmButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F2F2F2"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Background" Value="#FEE500"/>
                    <Setter Property="Foreground" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.15" ShadowDepth="1"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseDown="Header_MouseDown">
                <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                        Width="40" Height="40" Margin="0,5,10,0"
                        Background="Transparent" BorderThickness="0" 
                        Click="CloseButton_Click" Cursor="Hand">
                    <TextBlock Text="✕" FontSize="18" Foreground="#888888"/>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1" Margin="30,10,30,0">
                <TextBlock Text="잠금모드 암호 설정" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#111111" 
                           Margin="0,0,0,18"/>

                <TextBlock Text="잠금모드 해제 시 필요한 암호를 설정합니다.&#x0a;입력창에 암호로 사용할 4자리 숫자를 입력해 주세요." 
                           FontSize="13" 
                           Foreground="#555555" 
                           LineHeight="20"
                           Margin="0,0,0,35"/>

                <Grid Margin="0,0,0,25">
                    <TextBox x:Name="PasswordInput" 
                             Style="{StaticResource ModernTextBoxStyle}"
                             Tag="잠금모드 암호 입력"
                             MaxLength="4"
                             PreviewTextInput="Numeric_PreviewTextInput"
                             Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Password.Length, FallbackValue=0}" Foreground="#999999" FontSize="13"/>
                        <TextBlock Text="/4" Foreground="#999999" FontSize="13"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <TextBox x:Name="ConfirmPasswordInput" 
                             Style="{StaticResource ModernTextBoxStyle}" 
                             Tag="잠금모드 암호 확인"
                             MaxLength="4"
                             PreviewTextInput="Numeric_PreviewTextInput"
                             Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ConfirmPassword.Length, FallbackValue=0}" Foreground="#999999" FontSize="13"/>
                        <TextBlock Text="/4" Foreground="#999999" FontSize="13"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <Grid Grid.Row="2" Margin="30,0,30,25">
                <Button Content="확인" 
                        Style="{StaticResource ConfirmButtonStyle}"
                        Command="{Binding SetPasswordCommand}"/>
            </Grid>
        </Grid>
    </Border>
</Window>