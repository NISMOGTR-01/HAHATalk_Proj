<Window x:Class="HAHATalk.Views.ProfileDetailWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:HAHATalk.Views"
            mc:Ignorable="d" 
            Title="Profile"     
            Height="520" Width="380"
            WindowStyle="None"
            AllowsTransparency="True"
            Background="Transparent"
            ResizeMode="NoResize"
            WindowStartupLocation="CenterScreen">
    <Border CornerRadius="20" ClipToBounds="True">
        <Grid>
            <Image Source="{Binding BackgroundImage}" Stretch="UniformToFill">
                <Image.Effect>
                    <BlurEffect Radius="10"/>
                </Image.Effect>
            </Image>
            <Rectangle Fill="Black" Opacity="0.3"/>

            <!-- 창을 닫는 버튼 -->
            <Button Content="X" HorizontalAlignment="Right" 
                    VerticalAlignment="Top" Margin="20"
                    Background="Transparent" Foreground="White"
                    BorderThickness="0"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    />
                   

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Margin="0 50 0 0">
                <Ellipse Width="100" Height="100" Stroke="White"
                         StrokeThickness="2">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <TextBlock Text="{Binding UserName}" Foreground="White"
                           FontSize="20" FontWeight="BOld"
                           HorizontalAlignment="Center"
                           Margin="0 15 0 5"/>
                <TextBlock Text="{Binding StatusMessage}" 
                           Foreground="#DDFFFFFF" FontSize="14" 
                           HorizontalAlignment="Center"/>

            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Margin="0 0 0 40">
                <Separator Background="#55FFFFFF" Margin="30 0 30 20"/>
                <UniformGrid Columns="3">
                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Command="{Binding StartChatCommand}">
                        <StackPanel>
                            <Path Data="{StaticResource IconChat}" Fill="White" 
                                  HorizontalAlignment="Center"/>
                            <TextBlock Text="1:1 채팅" Foreground="White"
                                       FontSize="12" Margin="0 5 0 0"/>
                        </StackPanel>    
                    </Button>
                </UniformGrid>
            </StackPanel>
        </Grid>
    </Border>
 
</Window>
